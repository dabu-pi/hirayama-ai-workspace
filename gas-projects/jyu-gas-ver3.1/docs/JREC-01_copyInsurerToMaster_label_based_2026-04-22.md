# JREC-01 copyInsurerToMaster_V3 — ラベル名ベース転記への変更方針

**日付:** 2026-04-22  
**対象ファイル:** `Ver3_core.js`  
**変更種別:** リファクタリング（既存運用を壊さない最小修正）

---

## 変更前の問題点

| 問題 | 詳細 |
|---|---|
| セル番地固定参照 | `insurerSh.getRange(1, 2)` で患者IDを取得（B1固定） |
| 転記ロジックが硬直 | `FIELD_MAP` に書かれた項目しか転記できない |
| 表記ゆれに非対応 | 「確認日 :」のようなラベルを正規化していない |
| 転記漏れが分からない | Logger出力が不足。スキップ理由が「配偶者」と「列なし」だけ |
| 不足フィールド | 記号・枝番・本人家族の別・被保険者氏名が転記対象外 |

---

## 変更後の設計

### 補助関数 4本

| 関数名 | 役割 |
|---|---|
| `normalizeInsurerLabel_(label)` | 末尾の ` :` `：` を除去してラベルを正規化 |
| `getMasterHeaderMap_(sheet)` | 患者マスタ1行目から `{列名: 1-based列番号}` を構築 |
| `buildInsurerRecordByLabel_(sheet, blockStartCol)` | 保険者情報シートを `{正規化ラベル: 値}` 辞書に変換。重複ラベルは先頭優先 |
| `copyInsurerFieldsToMasterRow_(masterSheet, rowIndex, headerMap, insurerRecord)` | 辞書のキーと患者マスタ列名の一致で転記。Logger出力付き |

### 転記スコープの変更

| 旧 | 新 |
|---|---|
| `FIELD_MAP` に列挙した13項目のみ | insurerRecord のキーと headerMap の列名が**完全一致するすべて**の項目 |

→ 列追加・順番変更があっても自動対応。新しいラベルが保険者情報に加わればそのまま転記される。

### 患者ID取得方法の変更

| 旧 | 新 |
|---|---|
| `insurerSh.getRange(1, 2).getValue()` （B1固定） | `insurerRecord["照会番号"]` （ラベル名で取得） |

### Logger 出力（3種）

```
[copyInsurer] 転記した項目: 氏名, フリガナ, ...
[copyInsurer] 転記元にあるが患者マスタに列なし: 記号, ...
[copyInsurer] 転記対象なのに値が空: 枝番, ...
[copyInsurer] 必須フィールドが保険者情報シートに見つからない: 本人・家族の別  ← 保険者情報側に列がない場合
```

### 必須フィールド（REQUIRED_FIELDS）17項目

氏名 / フリガナ / 生年月日 / 性別 / 区分 / 負担割合 / 保険者番号 / 保険者名 / 記号 / 番号 / 枝番 / 本人・家族の別 / 被保険者氏名 / 有効開始日 / 資格取得年月日 / 確認日 / 要配慮情報（備考）

※ REQUIRED_FIELDS は転記を制限するリストではなく「ログ確認用のチェックリスト」。  
　 実際の転記はラベル一致で行われるため、REQUIRED_FIELDS 外の列も転記される。

---

## 配偶者情報の除外ロジック

旧: `seenOnce` で 氏名・性別 の2回目以降をスキップ  
新: `buildInsurerRecordByLabel_` が重複ラベルを先頭優先で格納するため、自動的に同じ効果を得る。`seenOnce` ロジックは削除。

---

## blockStartCol パラメータの意味

`buildInsurerRecordByLabel_(sheet, blockStartCol)` の `blockStartCol` は、ラベル列の1-based列番号。

| blockStartCol | ラベル列 | 値列 |
|---|---|---|
| 1 (既定) | A | B |
| 3 | C | D |

将来、保険者情報シートに複数患者ブロックが横並びになっても対応可能。

---

## 既存運用への影響

- `copyInsurerToMaster_V3` の関数名・メニュー配置は変更なし
- 転記結果ダイアログの内容に「患者マスタに列なし」「転記対象だが値が空」が追加
- 転記13項目（旧FIELD_MAP）はすべて引き続き転記される
- 新たに 記号・枝番・本人家族の別・被保険者氏名 が転記対象に追加（患者マスタに列があれば）

---

## 実機確認チェックリスト

> 実施前提: Apps Script エディタを開き、実行後に「実行数」→「ログを表示」で `[copyInsurer]` 行を確認できる状態にしておくこと。

### A. 基本動作確認（必須）

| # | 確認項目 | 合格条件 | 結果 |
|---|---|---|---|
| A-1 | B1 に実在する患者IDを入力して実行 | 結果ダイアログが表示される | ⬜ |
| A-2 | 結果ダイアログに「転記した項目（N件）」が表示される | N ≥ 1 | ⬜ |
| A-3 | 患者マスタの該当行に 氏名・フリガナ・生年月日・性別・区分・負担割合・保険者番号・保険者名・確認日 が書き込まれている | 転記後にセルを目視確認 | ⬜ |
| A-4 | Logger に `[copyInsurer] 転記した項目:` が出ている | Apps Script ログで確認 | ⬜ |

### B. 照会番号 ↔ 患者ID の対応確認 ✅ 解消済み（2026-04-22）

| # | 確認項目 | 合格条件 | 結果 |
|---|---|---|---|
| B-1 | 保険者情報 A1 のラベルが「照会番号」であること | 目視確認 | ⬜ |
| B-2 | 保険者情報 B1 の値（照会番号）が患者マスタの「患者ID」列の値と一致していること | 例: どちらも `P001` など | ⬜ |
| B-3 | 照会番号 ≠ 患者ID のケースが運用上存在しないか | **✅ 解消済み** — 下記注記参照 | ✅ |

> **運用確定（2026-04-22）:** マイナ確認アプリの「照会番号」欄にはユーザーが院内運用の患者IDを手入力している。よって `insurerRecord["照会番号"]` = 患者マスタの `患者ID` は常に一致する。別管理リスクは存在しない。

### C. ラベル表記ゆれ確認

| # | 確認項目 | 合格条件 | 結果 |
|---|---|---|---|
| C-1 | 保険者情報シートのA列ラベルに末尾「 :」「：」が付いているか目視確認 | 付いている場合 → 転記が成功することを A-3 で確認済み | ⬜ |
| C-2 | ラベルに全角スペース・前後空白が入っているか確認 | あれば `normalizeInsurerLabel_` が除去していることを Logger で確認 | ⬜ |
| C-3 | 「確認日」「確認日 :」「確認日：」のいずれのラベルでも同じ列に転記されること | Logger に `確認日` が `転記した項目` に出ていること | ⬜ |

### D. 項目追加・並び変更への耐性確認

| # | 確認項目 | 合格条件 | 結果 |
|---|---|---|---|
| D-1 | 保険者情報シートの行順を入れ替えて実行 | 転記結果が変わらない | ⬜ |
| D-2 | 保険者情報シートに存在しないラベルを1行追加して実行 | Logger に `患者マスタに列なし` が出るが転記は正常完了する | ⬜ |
| D-3 | 患者マスタに存在しない列名のラベルを保険者情報に追加して実行 | Logger `転記元にあるが患者マスタに列なし: <追加したラベル名>` が出る | ⬜ |

### E. Logger 出力確認（詳細）

| # | 確認項目 | 合格条件 | 結果 |
|---|---|---|---|
| E-1 | `[copyInsurer] 転記した項目:` が出ている | 項目名がカンマ区切りで列挙される | ⬜ |
| E-2 | 患者マスタに存在しない列のラベルがある場合 `[copyInsurer] 転記元にあるが患者マスタに列なし:` が出る | D-2/D-3 で確認 | ⬜ |
| E-3 | 値が空のフィールドがある場合 `[copyInsurer] 転記対象なのに値が空:` が出る | 保険者情報の任意フィールドを空にして実行 | ⬜ |
| E-4 | 保険者情報シートに「記号」「枝番」「本人・家族の別」「被保険者氏名」がない場合 `[copyInsurer] 必須フィールドが保険者情報シートに見つからない:` が出る | ラベルを削除して実行 | ⬜ |

### F. 新規転記項目の確認

| # | 確認項目 | 合格条件 | 結果 |
|---|---|---|---|
| F-1 | 患者マスタに「記号」列がある場合、保険者情報の「記号」が転記される | 目視確認 | ⬜ |
| F-2 | 患者マスタに「枝番」列がある場合、保険者情報の「枝番」が転記される | 目視確認 | ⬜ |
| F-3 | 患者マスタに「本人・家族の別」列がある場合、転記される | 目視確認 | ⬜ |
| F-4 | 患者マスタに「被保険者氏名」列がある場合、転記される | 目視確認 | ⬜ |
| F-5 | 上記列が患者マスタに存在しない場合、エラーなくスキップされる | ダイアログ正常表示、Logger に `列なし` が出る | ⬜ |

### G. エラーケース確認

| # | 確認項目 | 合格条件 | 結果 |
|---|---|---|---|
| G-1 | B1（照会番号）が空で実行 | アラート「患者IDが入力されていません」が出て処理停止 | ⬜ |
| G-2 | 存在しない患者IDを入力して実行 | アラート「患者IDが見つかりません」が出て処理停止 | ⬜ |
| G-3 | 保険者情報シートがない状態で実行 | アラート「保険者情報シートが見つかりません」が出て処理停止 | ⬜ |

---

## Logger 出力例（正常ケース）

保険者情報に17項目すべてあり、患者マスタにも全列ある場合の期待出力:

```
[copyInsurer] 転記した項目: 氏名, フリガナ, 生年月日, 性別, 区分, 負担割合, 保険者番号, 保険者名, 記号, 番号, 枝番, 本人・家族の別, 被保険者氏名, 有効開始日, 資格取得年月日, 確認日, 要配慮情報（備考）
```

患者マスタに「記号」「枝番」列がない場合の期待出力:

```
[copyInsurer] 転記した項目: 氏名, フリガナ, 生年月日, 性別, 区分, 負担割合, 保険者番号, 保険者名, 番号, 本人・家族の別, 被保険者氏名, 有効開始日, 資格取得年月日, 確認日, 要配慮情報（備考）
[copyInsurer] 転記元にあるが患者マスタに列なし: 記号, 枝番
```

保険者情報の「枝番」が空だった場合の期待出力:

```
[copyInsurer] 転記した項目: 氏名, フリガナ, ..., 枝番, ...
[copyInsurer] 転記対象なのに値が空: 枝番
```

保険者情報シートに「本人・家族の別」ラベルが存在しない場合の期待出力:

```
[copyInsurer] 必須フィールドが保険者情報シートに見つからない: 本人・家族の別
[copyInsurer] 転記した項目: 氏名, フリガナ, ...（本人・家族の別 を除く）
```

---

## Dashboard / Run_Log 反映要否

**不要。**

理由:
- 本変更はコードのリファクタリングのみ（転記アルゴリズムの置き換え）
- 帳票出力（申請書・施術録）への影響なし
- 保険算定ロジックへの影響なし
- 患者マスタの書き込み内容は変わらない（旧13項目は引き続き転記される）
- Run_Log は来院登録・金額計算の実行記録を対象としており、今回のマスタ転記補助操作は対象外

実機確認完了後、本 docs の各チェック項目に `✅` を記入し、PROJECT_STATUS.md の確認ステータスを更新する。

---

## 残リスク整理（2026-04-22 更新）

| リスク | 状態 | 備考 |
|---|---|---|
| 照会番号 ≠ 患者ID のケースが存在する | ✅ **解消済み** | マイナ確認アプリの照会番号欄にユーザーが院内患者IDを手入力する運用が確定。別管理リスクなし |
| 患者マスタに新規列（記号・枝番・本人家族の別・被保険者氏名）が未追加 | ⏳ **シート確認待ち** | 列が存在しなければ転記されずLogger に「列なし」が出る。実機で確認後、必要な列を追加する |
| 実行時 Logger が想定通り出るか | ⏳ **シート確認待ち** | 正常・列なし・値空・必須欠落の4パターンを実機で確認する（チェックリスト E-1〜E-4） |

---

## 次にシートで行う確認手順（2026-04-22 時点の残確認）

> 前提: 照会番号 = 患者ID の運用が確定済み。B-3 チェックは完了扱い。

**手順 1 — 患者マスタの列を確認する**

患者マスタを開き、以下の列が存在するか確認する。

| 列名 | 確認 |
|---|---|
| 記号 | ⬜ |
| 枝番 | ⬜ |
| 本人・家族の別 | ⬜ |
| 被保険者氏名 | ⬜ |

→ 存在しない列は転記されない。必要なら列を追加してから手順2へ進む。

**手順 2 — 転記を実行して Logger を確認する**

1. 保険者情報シートの B1 に実在する患者IDを入力
2. メニュー「管理」→「保険者情報を患者マスタへ反映」を実行
3. 結果ダイアログで転記件数・スキップ項目を確認
4. Apps Script エディタ「実行数」→「ログを表示」で `[copyInsurer]` 行を確認

**期待する Logger 出力（参考）:**

```
[copyInsurer] 転記した項目: 氏名, フリガナ, 生年月日, 性別, 区分, 負担割合, 保険者番号, 保険者名, 記号, 番号, 枝番, 本人・家族の別, 被保険者氏名, 有効開始日, 資格取得年月日, 確認日, 要配慮情報（備考）
```

患者マスタに列がない項目がある場合は追加で:

```
[copyInsurer] 転記元にあるが患者マスタに列なし: 記号, 枝番
```

**手順 3 — 患者マスタの対象行を目視確認する**

- 氏名・フリガナ・生年月日・保険者番号・確認日 が正しく入っているか
- 手順1で確認した新規列（記号・枝番等）に値が入っているか
