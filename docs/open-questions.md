# 未解決事項・要確認事項

最終更新: 2026-04-05

## 高優先度の確認事項

| ID | 分類 | 不明点/要確認事項 | なぜ重要か | 現時点の状況 | 次アクション |
|---|---|---|---|---|---|
| OQ-001 | GAS | `ネットショップ商品一覧2018-10-22` に紐づく Apps Script の scriptId、トリガー、デプロイ状態 | 商品コード生成、WordPress/BASE出力、サイト反映の停止/移行順序を決めるため | `ネットショップ商品一覧GAS.txt` で処理内容は確認できたが、scriptId と実トリガーは未取得 | ブラウザで対象ブックを開き、`拡張機能 > Apps Script` から scriptId とトリガーを取得 |
| OQ-002 | GAS | `【見積】見積もりテンプレート2.3` / `2.3freee連携API` / `【見積】長谷川様ご依頼分` に紐づく Apps Script の scriptId と、提供テキストがどのブック版か | どの見積ブックを正本として移植するか、どの版を止めるかを判断するため | `見積もりテンプレートGAS.txt` で `myOnEdit`/`mainFunc` の処理内容は確認できたが、どのブックに同一コードが入っているか未確定 | 各ブックの Apps Script を開き、コード一致・トリガー有無・更新日を照合 |
| OQ-003 | GAS | `STRONGDEPOT 競合サイトデータ` の収集スクリプト scriptId、実行トリガー、運用頻度 | 競合収集を止めずに新パイプラインへ移すため | `競合サイトGAS.txt` で取得/画像保存ロジックは確認できたが、定期実行設定は未取得 | Apps Script のトリガー画面で時間主導トリガーの有無と頻度を確認 |
| OQ-004 | サイト反映 | `https://machine-group.net/strongdepot-product-manager/generate.php` の内部で WordPress へどう反映しているか、`Wordpress用csv` と JSON POST の使い分け | WordPressを外す移行ステップと置換対象が変わる | GAS から `generate.php` へ POST する経路は確認できたが、PHP 側実装は未確認 | `strongdepot-product-manager` の `generate.php` と `Settings.php` を回収して処理内容を棚卸し |
| OQ-005 | 正本確認 | `ネットショップ商品一覧2018-10-22` が現行商品マスタ正本か、`2024` や `3.24bk` やコピーが使われていないか | 誤ったブックを新システムの入力元にすると移行データがずれる | 更新日時から `2018-10-22` が有力だが確定ではない | 現場確認、最終更新者/リンク元/Apps Script参照先の照合 |
| OQ-006 | 商品コード仕様 | `SD商品コード` の正式な構成、連番採番ルール、再採番禁止ルール、メーカーコード桁数 | 新システムで商品コード互換を維持するため | 実データと `ルール` の説明に一部ずれが見える | 商品コード生成GAS取得 + 代表商品コードの分解表作成 |
| OQ-007 | 見積正本 | `見積もりテンプレート2.3`、`2.3freee連携API`、案件別見積集約のどれを新規見積の起点として使っているか | 新システムでどの帳票/ロジックを正として移植するか決まらない | いずれも現役の可能性があり、関係が未確定 | 実運用手順を確認し、1件の見積作成フローをトレース |
| OQ-008 | 見積参照元 | 見積テンプレートが商品コードからどのシート/ブックを参照して商品情報を取得しているか | 商品マスタ連動を新システムで落とさないため | `mitsumori` に商品コード列はあるが参照式/GASは未特定 | 見積ブックの数式・名前付き範囲・GASを確認 |
| OQ-009 | BASE | BASE用CSVは現在も業務で使っているか | 新システムでチャネル出力として残すか、後回しにするか判断するため | `BASE用csv` タブはあるが運用頻度不明 | 現場確認、最近更新行の有無を確認 |
| OQ-010 | その他商品一覧 | `その他商品一覧` は現役か、どの業務で使うか | 新商品マスタへ統合するかアーカイブするか判断するため | 見出しのみ確認、データ行は見えず | GAS参照有無と運用実態を確認 |
| OQ-023 | その他商品マスタ | 見積GASが参照する `【商品追加用ページ】メーカー、同業者` を新システムの商品マスタへどう統合するか | SD商品マスタと別系統の商品マスタがあり、見積入力経路が2本に分かれているため | `その他の商品一覧` / `TEST商品` / `画像` の3タブ構成と、`その他の商品一覧` の列構成は確認済み | `TEST商品` と `画像` の実データ・運用頻度を確認し、統合先ドメインを決める |

## 競合データ系の確認事項

| ID | 分類 | 不明点/要確認事項 | 現時点の状況 | 次アクション |
|---|---|---|---|---|
| OQ-011 | 競合分類 | `競合サイトデータまとめ` の `メーカー分類` で `#ERROR!` が出る原因 | `E2:E10` が errorValue になっていることを確認 | 数式/参照範囲/権限/カスタム関数の有無を確認 |
| OQ-012 | 競合分類 | `カテゴリー分類` タブの実データ範囲と役割 | A1:Z30 では値が取得できず不明 | より広い範囲・別列・フィルタ/数式を確認 |
| OQ-013 | 競合中継 | `他社競合データ` をなぜ `IMPORTRANGE` 中継にしているか | 中継式は確認できたが理由は不明 | 参照先シートや使っている下流ブックを確認 |
| OQ-014 | 画像保存 | 競合画像のDriveフォルダ `1Q0vGVu2N8Ouq8us0JIMSaH1oCdHVLiZl` の権限、命名、重複処理 | マニュアルの保存先と命名規則は確認済み | 実ファイル構成と再取得時の上書きルールを確認 |

## 案件・見積・周辺業務の確認事項

| ID | 分類 | 不明点/要確認事項 | 現時点の状況 | 次アクション |
|---|---|---|---|---|
| OQ-015 | 案件台帳 | `2024長谷川さん` の各列の正式定義、固定列変更可否、入力担当 | `freee-automation` が Q/R/S/T 等へ依存している可能性が高い | 列仕様書を作り、GAS参照と突き合わせる |
| OQ-016 | lines_json | `lines_json作成` のカスタムメニュー実装がどのGASにあるか | シート側の説明は確認できたがメニュー関数の所在は未特定 | ブック紐づきGASとローカルGASの関数名を突き合わせる |
| OQ-017 | 案件範囲 | `きらく苑`、`2024廃棄処理`、`スミスマシン仕様書`、`アルバイト給与明細` を新システム範囲に含めるか | 中古マシン販売本流と別業務が混在している | Phase分けして対象範囲を決める |
| OQ-018 | 個人情報 | 顧客メール、氏名、給与情報などをどのブック/列まで新システムへ持ち込むか | 現行シートに個人情報が広く混在 | データ最小化方針と閲覧権限設計を決める |

## 壊れている/不整合の可能性がある点

| ID | 事象 | 現時点の確認内容 | 影響 | 次アクション |
|---|---|---|---|---|
| OQ-019 | 旧見積テンプレートリンク切れ | `2020マシンやグループ全案件進捗状況` の `リンク` タブにある旧テンプレートID `1ZM5veZcu-WGifslyCRkBtQwjigSUiAccqvDOypkZ_zs` は 404 | 過去運用導線や旧資料参照が欠損している可能性 | 代替ブックがあるか確認し、リンク集を更新/注記 |
| OQ-020 | メーカーコード仕様の説明不一致 | `ルール` シートにメーカーコード「3桁」と見えるが、実商品コードは `CY` など2文字に見える | コード再実装時に仕様を誤る可能性 | 商品コード生成GASと過去コード一覧から正式仕様を確定 |
| OQ-021 | タイムゾーン不一致 | `ネットショップ商品一覧2018-10-22` のタイムゾーンが `America/Los_Angeles` | 日付処理やトリガー実行時刻のずれ | 現行GASの日時依存有無を確認し、新系では Asia/Tokyo 統一を検討 |
| OQ-022 | 競合分類エラー | `メーカー分類` のメーカー列が `#ERROR!` | 競合価格判断の信頼性低下 | 数式/カスタム関数/参照元シートを確認 |
| OQ-024 | 分類マスタ二重/三重管理 | 店舗/メーカー/部位/状態/カテゴリが `ルール` シート、商品一覧GAS配列、PHP `Settings.php` に分散している | どれか1箇所だけ更新するとコード生成やサイトカテゴリがずれる | GAS側配列と `Settings.php` 更新コメントは確認済み。PHP側実体は未取得 | 3者の差分表を作り、次世代では単一マスタへ統合する |
| OQ-025 | Apps Script API権限 | `.clasprc.json` の refresh_token で access_token を更新しても `processes:listScriptProcesses` が `403 ACCESS_TOKEN_SCOPE_INSUFFICIENT` になる | CLI/APIから scriptId / トリガー一覧を自動回収できず、手作業確認が残る | `script.googleapis.com` への直接呼び出しで 403 を確認済み | 必要なら別OAuthスコープで再ログインするか、まずは Apps Script エディタURLを人手回収する |
| OQ-026 | PHPソース所在 | `strongdepot-product-manager` の `generate.php` / `Settings.php` が GitHubコネクタ・公開GitHub URL・OneDrive控えで見つからない | WordPress反映処理の副作用とDB更新先を確定できない | `git ls-remote https://github.com/kohakuwebdesign/strongdepot-product-manager.git` は `Repository not found.`、OneDrive配下にもPHP控え無し | サーバー側配置場所、別リポジトリ名、または権限付きGitHub URLを確認する |

## 今回わかったこと

- 現行再構築の最大ブロッカーは、コンテナバインドGASの未取得と、どのブック/テンプレートが現行正本かの未確定。
- 商品コード仕様、見積正本、WordPress反映経路、競合分類エラーは、次フェーズの設計前に優先して潰すべき論点。

## まだ不明なこと

- Apps Script 本体、トリガー、実行メニュー、サイト反映経路
- コピー/バックアップ/試作ブックの現役度
- BASE継続要否、委託/代理販売/廃棄処理/給与明細まで新システムに含めるかの業務スコープ

## 次の一手

1. OQ-001〜OQ-008 を優先し、GAS取得と正本確認を進める。
2. 正本が確定したら、商品コード・見積計算・競合分類の仕様を Markdown 化する。
3. 新統合スプレッドシート v0 の列定義と現行→新スキーマのマッピング表を作る。

## すぐ着手できる実装候補

- `OQ-001〜OQ-022` をそのままチェックリスト化した調査管理シート
- 商品コード代表例の分解表
- 現行見積テンプレートの1案件トレース表
- 競合分類エラーの原因切り分けメモ

## 2026-04-04 設計フェーズ4 追加論点

| ID | 分類 | 未確定事項 | 影響 | 次の確認アクション | 優先度 | 関連ドキュメント |
|---|---|---|---|---|---|---|
| OQ-027 | 商品コード/設定マスタ | メーカー `MC` 衝突の新規採番ルール | `MAXICAM` と `MUSCLE CLAMP` が同じ旧コード `MC` のため、新規 `sd_product_code` 自動採番で曖昧になる | 新規採番時に旧コード `MC` を維持するか、新コードを別途割り当てるか業務判断する | 高 | `docs/settings-master-v0.md`, `docs/product-code-validation-spec.md` |
| OQ-028 | 商品コード/部位マスタ | `首` の旧部位コード空欄を新規採番でどう扱うか | 互換維持を優先すると空コードが残り、検索/検証が不安定になる | 既存コード保持と新規採番ルールを分け、`NECK` の非空コードを使うか決める | 中 | `docs/settings-master-v0.md`, `docs/product-code-validation-spec.md` |
| OQ-029 | サイト公開 | 売却済み商品のサイト掲載方針 | `publish_status` / `is_published` / `inquiry_enabled` の判定が変わる | 売却済みを非公開にするか、実績として公開し問い合わせ停止にするか現場ルールを確認する | 中 | `docs/site-output-view-v0.md` |
| OQ-030 | 競合価格 | 競合価格の税込/税抜扱い | 自社税抜売価との価格差分計算がずれる可能性がある | recyfit 表示仕様と現行判断基準を確認し、`*_ex_tax` 列でよいか確定する | 中 | `docs/competitor-data-v0.md` |
| OQ-031 | products.json | `products.json` に非公開商品を含めるか | 公開配信用JSONと管理検証用JSONの境界が変わる | 実装方式比較フェーズで「公開JSON」と「検証JSON」を分けるか決める | 中 | `docs/products-json-spec.md` |
| OQ-032 | 商品マスタ | `stock_quantity` の初期値ルール | 中古単品中心なら `1` 固定で足りるが、備品/複数台在庫では実数管理が必要 | カテゴリ別に `1` 固定/実数入力/空欄禁止のどれを採るか確認する | 低 | `docs/product-master-v0.md` |
| OQ-033 | 商品画像 | 表示用700x700正方形画像の保存先、余白背景色、再生成タイミング、サムネイル派生要否 | 元画像と表示用派生画像の役割は固まったが、実装方式によってURL形式や更新フローが変わる | Drive派生画像、オブジェクトストレージ、CDNのどれに置くか、背景色を白固定にするか、元画像差し替え時に即時/バッチどちらで再生成するかを実装フェーズで決める | 高 | `docs/image-spec-v0.md`, `docs/site-output-view-v0.md`, `docs/products-json-spec.md` |
