# 画像生成フェーズ計画

最終更新: 2026-04-05

## 現在の判断

700x700 派生画像生成の実装には、まだ進まない。

理由:

- 現行 `画像1〜3` は正本URLとして使えない
- `source_image_count=0` が 924件すべて
- 自社商品画像の正本保管先が未確定
- `machine-group.net` の公開 WordPress メディアは回収元候補として見えてきたが、まだ全件適用性を確認できていない

## 進行条件

| 条件 | 現状 | 判定 |
|---|---|---|
| 元画像の正本が分かる | WordPress 公開メディアが回収元候補として浮上、将来正本は未確定 | 未達 |
| 元画像の取得方法が再現できる | 公開HTMLからの回収仮説あり、全件検証前 | 一部達成 |
| 1商品に対して画像群を結びつけられる | `slug` と画像ファイル名に商品コードが見える | 一部達成 |
| 画像0件商品をどう扱うか決まっている | warning方針のみ | 一部達成 |
| 派生画像の保存先候補が整理されている | Google Drive を第一候補に整理 | 一部達成 |

## 再開時の入力

- 自社商品画像の正本一式
- 商品コードと画像ファイル名の対応ルール
- `product_master_v0.full.csv`
- 今後の保存先ルール

## 再開時の出力

- `data/derived-images/` 配下の 700x700 表示用画像
- 画像生成ログ
- `products.json.images[].displayUrl`

## 想定構造

- 元画像置き場: `data/raw-images/`
- 派生画像置き場: `data/derived-images/`
- 将来の表示用命名案: `{sd_product_code}-{sortOrder:02d}-700x700.jpg`

## 正本候補の現時点整理

- 自社商品画像:
  - `machine-group.net` の公開 WordPress メディアが回収元候補として最有力
  - `/products/<小文字商品コード>/` と `wp-content/uploads/<大文字商品コード>_枝番.<拡張子>` の対応が見えている
  - ただし、非公開商品まで拾えるか、公開HTMLだけで全件回収できるかは未確認
  - 今後の正式な正本保存先は引き続き Google Drive を第一候補とする
- 競合画像:
  - 既存 docs から Google Drive フォルダ `1Q0vGVu2N8Ouq8us0JIMSaH1oCdHVLiZl` に `商品ID_画像枝番` 形式で保存していた痕跡あり
  - 自社画像とは保管系統を分けて扱う前提が妥当

## 次に必要な調査

1. `strongdepot-product-manager` の PHP / WordPress 側実装を回収する
2. `machine-group.net` の公開側で 5〜10 商品の小規模回収テストをする
3. 過去の WordPress メディアまたはローカル控えに、商品コードを含む画像ファイルが残っているか確認する
4. Google Drive に自社商品画像フォルダが存在するか確認する
5. 競合画像と自社画像の正本を同じ設計に載せるか、別管理にするか決める

## 進める / 止める判断基準

進める:

- 公開または回収済み WordPress 画像の実体が取得できる
- 商品コード単位で画像群を拾える
- 試験的に 5〜10商品で原本と派生画像を対応付けできる
- 公開側だけでは足りない商品群の扱い方が決まる

止める:

- WordPress 公開側では一部しか拾えず、非公開商品の回収経路も見つからない
- 商品コードと画像ファイルの対応が曖昧
- URLだけではなくログイン必須・手作業必須で再現不能

## 今回まだ決めないこと

- 背景余白色
- JPEG / PNG / WebP の最終形式
- CDN / 公開URLの最終構成
- 再生成トリガーの最終方式
