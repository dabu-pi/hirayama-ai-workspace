# PROJECT_STATUS

最終更新: 2026-04-11

## 現在地

- 新規プロジェクト `training-program-platform-jp` を立ち上げ済み
- 実装はまだ開始していない
- 初期設計文書の整備と、`今日のワークアウト` 画面の wireframe（画面設計図）固定まで完了

## 完了済み

- ルート構成作成
  - `README.md`
  - `ROADMAP.md`
  - `docs/`
  - `app/`
  - `web/`
  - `admin/`
  - `assets/`
  - `seed/`
- 基本文書整備
  - 企画
  - 要件
  - ユーザーストーリー
  - 画面一覧
  - データモデル
  - スコープ
  - 用語集
  - 技術判断
  - 初期プログラム候補
- 実行 UI の中心要件整理
  - `Previous` を各セット行で即表示
  - 履歴を必須導線として扱う
  - 種目名タップで種目単体履歴へ遷移
  - Kg 初期空欄
  - 1 セット目入力後の後続セット自動反映
  - チェック後ロック
  - `Previous` なし時は `-`
- `今日のワークアウト` の wireframe（画面設計図）作成
  - 上部固定エリア
  - プログラム情報エリア
  - 種目ブロック
  - セット一覧
  - 補助操作
  - 画面下部 `Add Exercise`

## 次アクション

1. `今日のワークアウト` の interaction spec（操作仕様書）を作る
2. `種目単体履歴` 画面の wireframe（画面設計図）を作る
3. `Add Set / Swap / Add Exercise` の MVP 実装範囲を決める
4. その後に UI プロトタイプへ進む

## 保留事項

- `Add Set / Swap / Add Exercise` を MVP 本実装に含めるか
- ロック解除 UI の見せ方
- 後続セット自動反映後の個別上書きルール
- 履歴画面の詳細レイアウト
- `Previous` の参照単位
- `is_locked` を persist（保存して残す）するかどうか
- 既存有名プログラムの公開時の権利 / 表現方針

## テスト状況

- コード実装は未着手
- 今回は設計文書のみ作成 / 修正
- 目視で文書整合性を確認済み

## 直近の重要判断

- このアプリの最重要画面は `今日のワークアウト`
- `前回記録` と `履歴` は補助機能ではなく MVP の中核
- `Previous` は各セット行で即見える
- 履歴導線は必須とし、種目名タップは種目単体履歴へ遷移
- スマホ縦画面を最優先とする
