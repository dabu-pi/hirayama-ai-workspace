# 開発ロードマップ

平山克司ワークスペース — 全プロジェクト統合ロードマップ
作成: 2026-03-05

---

## 全体マップ

```
2026年3月                    4月                      5月〜
│
├─ 【柔整GAS】───────────────────────────────────────────────────→
│     施術明細upsert実装 → テスト通過 → 申請書生成安定稼働
│
├─ 【freee自動化】───────────────────────────────────────────────→
│     OAuth再構築 → フェーズ2完成 → フェーズ3(下書き) → 運用強化
│
├─ 【患者管理Web】──────────────────────────────────→
│     認証整備 → 柔整GASと連携 → 検索・可視化
│
├─ 【廃棄物日報GAS】────────────────────────────────────────────→
│                              要件定義 → 設計 → 実装
│
└─ 【接骨院戦略AI】─────────────────────────────────────────────→
                              数値入力 → Claude API実装 → 月次レポート
```

---

## プロジェクト別ロードマップ

---

### 1. 柔整GASプロジェクト `gas-projects/jyu-gas-ver3.1/`

**現状:** 来院登録・金額計算・来院ヘッダ追記（ステップ①〜③）は稼働中。
ステップ④（施術明細upsert）が未実装のため、療養費支給申請書への転記が完結していない。

#### フェーズ A — 施術明細upsert完成（最優先）

| # | タスク | ファイル | 内容 |
|---|---|---|---|
| A-1 | `calcOnePartAmount_V3_` の戻り値を内訳オブジェクト化 | `Ver3_core.js` | 数値1つ → `{base, cold, warm, electro, taiki, coef, total, byomei, bui...}` |
| A-2 | `calcCaseDetailAmount_V3_` を `{total, parts[]}` 形式に変更 | `Ver3_core.js` | 部位ごとの内訳を上位に渡せるようにする |
| A-3 | `calcHeaderAmountsByVisitKey_V3_` に `amounts.details` を追加 | `Ver3_core.js` | 内訳を申請書転記まで伝搬させる |
| A-4 | `saveVisit_V3` ステップ④を実装（施術明細シートへのupsert） | `Ver3_core.js` | `detailID = visitKey + "_C{n}_P{n}"` で冪等upsert |
| A-5 | `initSupport` キー名を修正 | `Ver3_transferData.js` | `"初検時相談支援"` → `"初検時相談支援料"` |

#### フェーズ B — テスト・品質保証

| # | タスク | 内容 |
|---|---|---|
| B-1 | TC01〜TC10 を実際のスプレッドシートで実行・通過確認 | TESTCASES.md 全10ケース |
| B-2 | 施術明細→申請書転記データの出力確認 | 実データで申請書生成テスト |
| B-3 | SPEC.md §14（ステップ④）の仕様を実装内容に合わせて更新 | ドキュメント整合 |

#### フェーズ C — 将来拡張

| # | タスク |
|---|---|
| C-1 | 月次レポート自動生成（請求データ集計） |
| C-2 | 接骨院戦略AIへの患者数・売上データ連携 |
| C-3 | 患者管理Webアプリとの患者IDスキーマ共通化 |

---

### 2. freee見積自動化 `freee-automation/`

**現状:** フェーズ1（メール→スプレッドシート自動記録）は動作済み。
フェーズ2（freee見積書作成）は freee 本番OAuth の `invalid_grant / 401` エラーで詰まり中。

#### フェーズ 1 — 完了済み ✅

- hawk メール検知・A/B/C/D/E/O列 自動記録
- Gmailラベル付与による重複防止
- Message-ID による冪等処理

#### フェーズ 2 — OAuth再構築・見積書作成（最優先）

| # | タスク | 内容 |
|---|---|---|
| 2-1 | OAuth再構築 | `reset()` でトークン全削除 → `redirect_uri` をGAS WebアプリURL `/exec` と完全一致させて再認証 |
| 2-2 | 取引先検索・自動作成 | B列（顧客名）→ freee `partner_id` を自動解決。未登録なら自動作成してP列に保存 |
| 2-3 | 見積書POSTの完成 | Q列（lines_json）をもとに freee `/iv/api/1/quotations` へPOST |
| 2-4 | 成功時の自動記録 | G列（見積日）・R列（quotation_id）をシートに保存 |
| 2-5 | 未決事項の確認 | hawk全メール処理か件名フィルタか / 発生日ルール / 明細不足時の例外設計 |

#### フェーズ 3 — Gmail下書き作成

| # | タスク | 内容 |
|---|---|---|
| 3-1 | 見積PDF取得 | freee API から見積PDF を取得 |
| 3-2 | 返信メール下書き生成 | PDF添付 + 返信文テンプレートで下書き保存（送信しない） |
| 3-3 | 暫定案（PDF困難な場合） | freee 見積URL を本文に貼る形式 |

#### フェーズ 4 — 運用強化

| # | タスク |
|---|---|
| 4-1 | エラー通知（Slack / メール）の実装 |
| 4-2 | 取引先マスタの整備（顧客名 → freee取引先ID 対応表） |
| 4-3 | 品目マスタの整備（定型品目の単価・税区分） |
| 4-4 | 重複検知（同一顧客・同一件名の二重見積防止） |

---

### 3. 患者管理Webアプリ `patient-management/`

**現状:** 患者登録・編集・削除・施術記録追加・詳細表示の全ルートが実装済みのプロトタイプ。
`service_account.json` の管理が未整備。柔整GASとの連携は未設計。

#### フェーズ A — 本番化準備

| # | タスク | 内容 |
|---|---|---|
| A-1 | 認証情報管理の整備 | `service_account.json` のパスを環境変数（`.env`）経由に変更 |
| A-2 | エラーハンドリングの強化 | Sheets API失敗時のフォールバック・ユーザー向けメッセージ |
| A-3 | `flask_log.txt` の出力先を整理 | ログをファイルではなく標準出力へ |

#### フェーズ B — 柔整GASとの連携

| # | タスク | 内容 |
|---|---|---|
| B-1 | 患者IDスキーマの共通化 | 柔整GAS の `患者ID` フォーマットと合わせる |
| B-2 | 患者検索・フィルタリング機能 | 氏名・生年月日・住所でのインクリメンタル検索 |
| B-3 | 来院履歴の表示 | 柔整GASの「来院ヘッダ」シートから来院日・区分・金額を取得して表示 |

#### フェーズ C — 可視化・拡張

| # | タスク |
|---|---|
| C-1 | 月別来院数グラフ（Chart.js等） |
| C-2 | 患者ステータス管理（アクティブ / 休眠 / 終了） |

---

### 4. 廃棄物日報GASシステム `waste-report-system/`

**現状:** 企画段階。ディレクトリ未作成。

#### フェーズ A — 要件定義

| # | タスク |
|---|---|
| A-1 | 収集業務フローのヒアリング（収集品目・収集先・記録頻度） |
| A-2 | 法定書類フォーマットの確認（提出先・必須項目） |
| A-3 | 既存の日報様式（紙・Excel等）の収集 |

#### フェーズ B — 設計・実装

| # | タスク | 内容 |
|---|---|---|
| B-1 | スプレッドシート設計 | 日報入力シート・集計シート・マスタシートの構成 |
| B-2 | 日報入力UIの実装 | GASフォーム or スプレッドシートUI |
| B-3 | 月次集計の自動化 | 品目別・収集先別の集計ロジック |
| B-4 | PDF出力 | 日報・月報の印刷用PDF生成 |

---

### 5. 接骨院戦略AI `hirayama-jyusei-strategy/`

**現状:** 戦略・メニュー・マーケティング・財務のドキュメントは整備済み。
コスト構造・利益シミュレーションの数値が未入力。Claude APIの実装は未着手。

#### フェーズ A — データ入力（すぐできる）

| # | タスク | ファイル |
|---|---|---|
| A-1 | 固定費・変動費の実数値を入力 | `finance/cost-structure.md` |
| A-2 | 治療機器投資額・ジム設備費を入力 | `finance/equipment-investment.md` |
| A-3 | 現状の月次患者数・売上を入力 | `finance/profit-simulation.md` |

#### フェーズ B — Claude API実装

| # | タスク | 内容 |
|---|---|---|
| B-1 | データ読み込みスクリプト | スプレッドシート（柔整GAS）から月次患者数・売上を取得するPythonスクリプト |
| B-2 | 分析プロンプト設計 | LTV試算・損益分岐点・集客施策提案のプロンプトを設計 |
| B-3 | 月次レポート生成 | Claude API (`claude-sonnet-4-6`) でMarkdownレポートを自動生成 |
| B-4 | PDF出力 | Markdown → PDF変換（WeasyPrint等） |

#### フェーズ C — 継続運用

| # | タスク |
|---|---|
| C-1 | 月次自動実行（GAS or cronで月初に実行） |
| C-2 | KPIダッシュボード（患者数推移・自費比率・LTV） |

---

## マイルストーン

| 時期 | マイルストーン |
|---|---|
| **3月中旬** | 柔整GAS: 施術明細upsert完成・TC01〜TC10 通過 |
| **3月下旬** | freee自動化: OAuth再構築完了・フェーズ2（見積書作成）動作確認 |
| **4月上旬** | freee自動化: フェーズ3（Gmail下書き）完成 |
| **4月中旬** | 患者管理Webアプリ: 本番化準備完了・柔整GASと患者ID連携 |
| **4月下旬** | 廃棄物日報GAS: 要件定義完了・設計開始 |
| **5月** | 接骨院戦略AI: 数値入力完了・Claude APIで月次レポート初回生成 |
| **6月以降** | 全システム安定稼働・KPIダッシュボード構築 |

---

## 着手優先順位

```
🔴 今すぐ（稼働中システムの完成）
   1. 柔整GAS: 施術明細upsert実装（A-1〜A-5）
   2. freee自動化: OAuth再構築（2-1）

🟡 次に（業務効率化の直接効果）
   3. freee自動化: フェーズ2完成（2-2〜2-5）
   4. freee自動化: フェーズ3 下書き作成（3-1〜3-3）
   5. 患者管理Webアプリ: 認証整備・柔整GAS連携（A-1〜B-3）

🟢 後半（新規開発・中長期）
   6. 接骨院戦略AI: 数値入力 → Claude API実装
   7. 廃棄物日報GAS: 要件定義 → 設計 → 実装
```

---

最終更新: 2026-03-05
